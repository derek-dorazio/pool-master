# Session Handoff

## Resume Here

- next major product lane is contest team entry UI
- current commissioner contest create/manage flow is in place
- contest lifecycle is automatic from event/feed timing, not commissioner-driven
- Beads is now live in the repo and should be used for active slice tracking
  and stable refinement question IDs on larger lanes

## Context Notes

- commissioner contest configuration now uses relative lock timing in the UI
- draft contests can be managed or deleted before the event begins
- `requirements/` and `tech-specs/` are now available as design-input layers,
  while Beads is the live execution/refinement tracker and `plans/` remain the
  narrative execution context
