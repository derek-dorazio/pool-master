/**
 * NFL Scoring Templates — DEFERRED.
 *
 * NFL player-based scoring (PPR, Standard, Half-PPR) is deferred.
 * NFL contests support only team-based formats: Survivor and Pick'em.
 * These don't require scoring templates (winners determined by game results).
 *
 * See: plans/deferred/contest-rules-deferred.md
 */

// No NFL scoring templates — NFL uses team-based survivor/pick'em only.
export const NFL_TEMPLATES = {} as const;
