/**
 * Test data factories — fishery (or similar) factories for all domain models.
 *
 * Usage:
 *   import { leagueFactory, contestFactory } from '../factories';
 *   const league = leagueFactory.build({ name: 'My League' });
 */

export {};
