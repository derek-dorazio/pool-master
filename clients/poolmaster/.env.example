# PoolMaster web app — local Vite environment
# Copy to .env.local when the web app needs a browser-side override.
#
# Leave commented to call the same browser origin/proxy. Set this when the
# Vite dev server should call the API directly.
# VITE_API_BASE_URL=http://localhost:3000
