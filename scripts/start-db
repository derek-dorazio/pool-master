#!/usr/bin/env bash
set -euo pipefail

DB_INSTANCE_IDENTIFIER="${DB_INSTANCE_IDENTIFIER:-poolmaster-qa-postgres}"
AWS_REGION="${AWS_REGION:-us-east-2}"

aws rds start-db-instance \
  --region "$AWS_REGION" \
  --db-instance-identifier "$DB_INSTANCE_IDENTIFIER"
